# 补充10：表达式和控制流

> 基于 C++ Primer 第五版（英文版）第 4–5 章 *Expressions* 和 *Statements* 补充。
>
> **前置要求：** 已完成第 01–08 周全部内容。本文系统化复习和深化 C++ 的运算符与控制流知识。
>
> **关联已有知识：** 第 01 周（基本输入输出、循环）、第 02 周（函数中的条件判断）、第 04 周（算法中的 lambda 表达式）、第 07 周（运算符重载的本质）。
>

## 目录

- [〇、C 语言回顾：表达式和控制流](#〇、C-语言回顾表达式和控制流)
- [一、运算符深入](#一、运算符深入)
    - [1.1 算术、关系和逻辑运算符](#11-算术、关系和逻辑运算符)
    - [1.2 赋值运算符](#12-赋值运算符)
    - [1.3 自增/自减运算符](#13-自增/自减运算符)
    - [1.4 条件运算符 `?:`](#14-条件运算符-?:)
    - [1.5 位运算符](#15-位运算符)
    - [1.6 `sizeof` 运算符](#16-sizeof-运算符)
    - [1.7 类型转换](#17-类型转换)
- [二、控制流语句深入](#二、控制流语句深入)
    - [2.1 `switch` 语句](#21-switch-语句)
    - [2.2 范围 `for` 循环（C++11）](#22-范围-for-循环（C++11）)
    - [2.3 `do while` 循环](#23-do-while-循环)
    - [2.4 `break`、`continue` 和 `goto`](#24-break、continue-和-goto)
- [三、运算符优先级速查](#三、运算符优先级速查)
- [四、补充总结](#四、补充总结)
    - [核心要点](#核心要点)
    - [与后续课程的联系](#与后续课程的联系)

---

## 〇、C 语言回顾：表达式和控制流

```c
// 算术运算
int a = 10, b = 3;
int sum = a + b, diff = a - b;
int prod = a * b, quot = a / b, rem = a % b;

// 控制流
if (a > 5) { }
for (int i = 0; i < 3; i++) { }

// C 的限制：
// 1. 没有 bool 类型（C99 之前）
// 2. 没有范围 for 循环
// 3. 没有 switch 的 fallthrough 标注（C++17 有 [[fallthrough]]）
// 4. 类型转换只能用 C 风格强制转换，不安全
```

---

## 一、运算符深入

### 1.1 算术、关系和逻辑运算符

```cpp
// 算术：+, -, *, /, %, - (一元负)
int a = 5, b = 2;
// 注意：% 只能用于整数类型
// (-m) / n 和 (-m) % n 的结果依赖于实现（C++11 规定向 0 截断）

// 逻辑与和逻辑或的短路求值
int *p = nullptr;
// 短路：p != nullptr 为 false，&& 右边不会执行
if (p != nullptr && *p == 42) { }
// 短路：左边为 true，|| 右边不会执行
if (true || /*expensive_check()*/) { }

// 关系运算符：<, <=, >, >=, ==, !=
boolalpha;  // 打印 true/false 而非 1/0
```

### 1.2 赋值运算符

```cpp
// 赋值运算符是右结合的
int i, j;
i = j = 42;   // j = 42, 然后 i = j

// 复合赋值运算符
i += 5;   // i = i + 5
i -= 3;   // i = i - 3
i *= 2;   // i = i * 2

// ⚠️ 常见陷阱：赋值 vs 比较
int x = 0;
// if (x = 42)  // 把 42 赋给 x，条件永远为 true！
//     cout << "这是 bug！" << endl;
// 正确写法：
if (x == 42) { }
```

### 1.3 自增/自减运算符

```cpp
int i = 0;

// 前置 ++：先加后用
++i;       // i=1

// 后置 ++：先用后加
i++;       // 输出1, i=2

// ⚠️ 不要在同一个表达式里多次修改同一个变量
// cout << i << " " << ++i << endl;  // 未定义行为！

// 建议：自增/自减单独作为一条语句
++i;   // 清晰，无歧义
```

### 1.4 条件运算符 `?:`

```cpp
int grade = 75;

// 条件运算符 (cond ? true_expr : false_expr)
string result = (grade >= 60) ? "及格" : "不及格";

// 可以嵌套（但不要过度嵌套，影响可读性）
string level = (grade >= 90) ? "优秀"
             : (grade >= 80) ? "良好"
             : (grade >= 60) ? "及格"
             : "不及格";

// 条件运算符的优先级很低，做输出时要加括号
// cout << "成绩: " << (grade < 60 ? "不及格" : "通过") << endl;
```

### 1.5 位运算符

```cpp
unsigned char bits = 0b00110101;  // C++14 二进制字面量

// ~ 按位取反
// << 左移, >> 右移
// & 按位与, | 按位或, ^ 按位异或

unsigned char mask = 0b00001111;

// 常见用法
unsigned long quiz1 = 0;
quiz1 |= 1UL << 27;     // 设置第27位
quiz1 &= ~(1UL << 27);  // 清除第27位
bool pass = quiz1 & (1UL << 27);  // 检查第27位
```

### 1.6 `sizeof` 运算符

```cpp
// sizeof 对数组和指针的区别
int arr[10];
int *p = arr;
// sizeof(arr) 是整个数组的大小（40 bytes）
// sizeof(p)   是指针本身的大小（8 bytes）

// 数组元素个数
constexpr size_t sz = sizeof(arr) / sizeof(*arr);
```

### 1.7 类型转换

```cpp
// 隐式转换
int ival = 3.541 + 3;  // 3.541 → 3, then 3+3=6

// C++ 强制类型转换（推荐！比 C 风格更安全、更明确）
double d = 3.14;
int i1 = static_cast<int>(d);     // 常规转换（编译期检查）

// const_cast：去掉/加上 const
const char *cs = "hello";
char *s = const_cast<char*>(cs);  // 去掉 const（⚠️ 不要修改原数据！）

// reinterpret_cast：位模式的重新解释（危险，少用）
long addr = reinterpret_cast<long>(s);

// dynamic_cast 用于运行时类型检查（见补充17 - RTTI）
```

| 转换类型 | 用途 | 安全性 |
|---|---|---|
| `static_cast` | 常规类型转换（数值、void* 等） | 编译期检查 |
| `const_cast` | 去掉/添加 const/volatile | ⚠️ 注意不要修改原 const 对象 |
| `reinterpret_cast` | 位模式重新解释 | ❌ 非常危险，少用 |
| `dynamic_cast` | 多态类型的运行时转换 | 运行时检查（有开销） |
| C 风格 `(type)` | 以上所有 + 更多 | ❌ 不推荐——太宽泛，难排查 |

---

## 二、控制流语句深入

### 2.1 `switch` 语句

```cpp
// switch 语句：比多个 if-else 更清晰
char grade = 'B';

switch (grade) {
    case 'A':
        // 优秀
        break;   // ⚠️ 不要忘记 break！否则会 fall through
    case 'B':
        // 良好
        break;
    case 'C':
        // 及格
        break;
    case 'D':
        // 需要努力
        break;
    default:
        // 无效的成绩
        break;
}

// 故意的 fall through（多个 case 共享代码）
char ch = 'a';
unsigned vowelCnt = 0;
switch (ch) {
    case 'a': case 'e': case 'i': case 'o': case 'u':
    case 'A': case 'E': case 'I': case 'O': case 'U':
        ++vowelCnt;
        break;
}

// C++17: [[fallthrough]] 标注故意的 fall through
// switch (x) {
//     case 1:
//         do_something();
//         [[fallthrough]];  // 明确告诉编译器：这是故意的
//     case 2:
//         ...
// }
```

> **switch 检查清单：**
> - `case` 标签必须是整数常量表达式
> - 每个 `case` 后面通常要有 `break`
> - 可以在最后一个 `case` 或 `default` 省略 `break`
> - 变量定义需要放在 `{ }` 块中

### 2.2 范围 `for` 循环（C++11）

```cpp
vector<int> v{1, 2, 3, 4, 5};

// 范围 for：读（不修改）
for (auto x : v) { }

// 范围 for：写（修改原容器中的元素）
for (auto &x : v) {
    x *= 2;
}

// 对 string 使用范围 for
string s = "Hello";
for (auto c : s) { }

// ⚠️ 范围 for 中不能修改容器大小（不能 push_back / erase）
// for (auto x : v) {
//     v.push_back(x);  // ❌ 未定义行为！
// }
```

### 2.3 `do while` 循环

```cpp
// do while：至少执行一次循环体
string password;
int attempts = 0;

do {
    // 模拟输入
    password = (attempts == 0) ? "wrong" : "correct";
    ++attempts;
} while (password != "correct" && attempts < 3);
```

### 2.4 `break`、`continue` 和 `goto`

```cpp
// break：跳出最近的循环或 switch
for (int i = 0; i < 10; ++i) {
    if (i == 5) break;      // 提前退出循环
}

// continue：跳过本次迭代的剩余部分
for (int i = 0; i < 10; ++i) {
    if (i % 2 == 0) continue; // 跳过偶数
}

// goto：无条件跳转（⚠️ 少用，只在跳出深层嵌套时有价值）
int matrix[3][3] = {{1, 2, 3}, {4, 5, 6}, {7, 8, 9}};
bool found = false;

for (int i = 0; i < 3; ++i) {
    for (int j = 0; j < 3; ++j) {
        if (matrix[i][j] == 5) {
            found = true;
            goto found_it;  // 直接跳出双重循环
        }
    }
}
found_it:
// found = true
```

---

## 三、运算符优先级速查

| 优先级 | 运算符 | 结合性 |
|---|---|---|
| 1 (最高) | `::` | 左 |
| 2 | `.` `->` `()` `[]` `++`(后) `--`(后) | 左 |
| 3 | `!` `~` `++`(前) `--`(前) `+`(一元) `-`(一元) `*`(解引用) `&`(取地址) `sizeof` `new` `delete` | **右** |
| 4 | `.*` `->*` | 左 |
| 5 | `*` `/` `%` | 左 |
| 6 | `+` `-` | 左 |
| 7 | `<<` `>>` | 左 |
| 8 | `<` `<=` `>` `>=` | 左 |
| 9 | `==` `!=` | 左 |
| 10 | `&` (按位与) | 左 |
| 11 | `^` | 左 |
| 12 | `\|` | 左 |
| 13 | `&&` | 左 |
| 14 | `\|\|` | 左 |
| 15 | `?:` | **右** |
| 16 | `=` `+=` `-=` 等 | **右** |
| 17 | `,` | 左 |

> **记忆口诀：** "算关逻条赋逗"（算术 → 关系 → 逻辑 → 条件 → 赋值 → 逗号），大多数左结合，只有一元、条件和赋值是右结合。

---

## 四、补充总结

### 核心要点

| 知识点 | 关键结论 |
|---|---|
| 短路求值 | `&&` 和 `||` 左侧为 false/true 时跳过右侧 |
| `switch` | 别忘 `break`；`case` 标签必须是常量；用 `{}` 包局部变量 |
| 范围 `for` | `for (auto &x : container)` — 修改用引用，只读用值或 const 引用 |
| `do while` | 至少执行一次；条件后面要有 `;` |
| 类型转换 | 用 `static_cast` 代替 C 风格转换 |
| 优先级 | 不确定时加括号——代码的可读性比简洁更重要 |

### 与后续课程的联系

- **第04周** 的 lambda 表达式会大量使用 `auto` 和范围 `for`
- **第06周** 的 `new`/`delete` 是运算符，遵循这里讲的优先级
- **第07周** 的操作符重载将重新定义本章中的运算符行为
