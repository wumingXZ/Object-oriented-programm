# 第六周：定义抽象数据类型 — 模板类

> 基于课件 `05-定义抽象数据类型_模板类.md`，参考 C++ Primer 第五版（英文版）第 16 章（模板）、第 12 章（动态内存）、第 13 章（拷贝控制）补充优化。
>
>
> **前置知识**：第四周（泛型函数）、第五周（类、封装、构造函数）；C 语言（malloc/free、指针）。

## 目录

- [〇、C 语言回顾：动态内存管理的痛点](#〇、C-语言回顾动态内存管理的痛点)
- [一、模板类基础：`Vec<T>` 的设计](#一、模板类基础Vec<T>-的设计)
    - [1.1 从使用者的角度看 `Vec`](#11-从使用者的角度看-Vec)
- [二、`new` 和 `delete` — C++ 的动态内存](#二、new-和-delete---C++-的动态内存)
- [三、拷贝控制 — 三个关键操作](#三、拷贝控制---三个关键操作)
    - [3.1 为什么需要拷贝控制？](#31-为什么需要拷贝控制？)
    - [3.2 三法则（Rule of Three）](#32-三法则（Rule-of-Three）)
- [四、`explicit` 关键字](#四、explicit-关键字)
- [五、完整示例：自定义动态数组](#五、完整示例自定义动态数组)
- [六、第六周总结](#六、第六周总结)
    - [动态内存管理速查](#动态内存管理速查)

---

## 〇、C 语言回顾：动态内存管理的痛点

C 语言方式——每个需要动态数组的地方都要手动管理：

```c
// C 语言：为每种类型各写一遍
typedef struct { int* data; int size; int capacity; } IntVec;
typedef struct { double* data; int size; int capacity; } DoubleVec;
typedef struct { char* data; int size; int capacity; } CharVec;
// ... 无穷无尽！

void intvec_push(IntVec* v, int val) {
    if (v->size == v->capacity) {
        v->capacity *= 2;
        v->data = realloc(v->data, v->capacity * sizeof(int));  // 容易忘记
    }
    v->data[v->size++] = val;
}
// 每种类型都要重复几乎相同的代码！
```

C++ 的模板类解决这个问题：**写一次，用于所有类型**。

---

<a id="一模板类基础vect-的设计"></a>
## 一、模板类基础：`Vec<T>` 的设计

### 1.1 从使用者的角度看 `Vec`

```cpp
// Vec<int> vi;        // 一个 int 的动态数组
// Vec<string> vs;     // 一个 string 的动态数组
// vi.push_back(42);   // 添加一个 int
// vs.push_back("hi"); // 添加一个 string

// 接下来我们实现简化的 Vec

template <class T>
class Vec {
public:
    // ===== 类型定义（标准容器约定）=====
    typedef T* iterator;
    typedef const T* const_iterator;
    typedef size_t size_type;
    typedef T value_type;
    typedef T& reference;
    typedef const T& const_reference;

    // ===== 构造函数 =====
    Vec() { create(); }
    explicit Vec(size_type n, const T& val = T()) { create(n, val); }
    Vec(const Vec& v) { create(v.begin(), v.end()); }  // 复制构造函数

    // ===== 析构函数 =====
    ~Vec() { uncreate(); }

    // ===== 赋值运算符 =====
    Vec& operator=(const Vec& rhs) {
        if (&rhs != this) {  // 检查自我赋值！
            uncreate();
            create(rhs.begin(), rhs.end());
        }
        return *this;
    }

    // ===== 访问 =====
    size_type size() const { return avail - data; }
    T& operator[](size_type i) { return data[i]; }
    const T& operator[](size_type i) const { return data[i]; }

    // ===== 迭代器 =====
    iterator begin() { return data; }
    const_iterator begin() const { return data; }
    iterator end() { return avail; }
    const_iterator end() const { return avail; }

    // ===== 动态增长 =====
    void push_back(const T& val) {
        if (avail == limit) grow();  // 空间不够 → 扩容
        unchecked_append(val);
    }

private:
    iterator data;   // 指向第一个元素
    iterator avail;  // 指向最后一个元素的下一个位置
    iterator limit;  // 指向分配内存的末尾

    // 内存管理辅助函数
    void create() { data = avail = limit = nullptr; }

    void create(size_type n, const T& val) {
        data = new T[n];
        avail = limit = data + n;
        for (size_type i = 0; i < n; ++i)
            data[i] = val;
    }

    void create(const_iterator b, const_iterator e) {
        size_type n = e - b;
        data = new T[n];
        avail = limit = data + n;
        copy(b, e, data);
    }

    void uncreate() {
        if (data) {
            delete[] data;  // 释放内存
        }
        data = avail = limit = nullptr;
    }

    void grow() {
        size_type new_size = max(size_type(2 * (limit - data)), size_type(1));
        iterator new_data = new T[new_size];
        iterator new_avail = copy(data, avail, new_data);

        uncreate();  // 释放旧内存
        data = new_data;
        avail = new_avail;
        limit = data + new_size;
    }

    void unchecked_append(const T& val) {
        *avail++ = val;  // 在当前位置构造，然后移动指针
    }
};

// ===== 使用 Vec =====
Vec<int> vi;
vi.push_back(10);
vi.push_back(20);
vi.push_back(30);
vi.push_back(40);

vi[1] = 99;

Vec<string> vs;
vs.push_back("Hello");
vs.push_back("World");
vs.push_back("!");
```

`★ Insight ─────────────────────────────────────`
模板类定义中的 `T` 是**类型占位符**。编译器不会为模板生成代码——只有当用户创建 `Vec<int>` 这样的**实例化**时，编译器才用 `int` 替换 `T`，生成一份真实的类代码。这就是为什么模板类的完整定义必须放在头文件中：编译器需要看到完整定义才能实例化。
`─────────────────────────────────────────────────`

---

<a id="二new-和-delete--c-的动态内存"></a>
## 二、`new` 和 `delete` — C++ 的动态内存

从 C 的 `malloc/free` 过渡到 C++ 的 `new/delete`：

```cpp
class Person {
    string name;
    int age;
public:
    Person(const string& n, int a) : name(n), age(a) {}
    ~Person() {}
    void greet() const {}
};

// C++ 方式：new 分配内存 + 调用构造函数
Person* p1 = new Person("Alice", 25);
p1->greet();

// 带初始值的 new
int* pi = new int(42);  // 分配并初始化为42
delete pi;

// 数组的 new[] 和 delete[]
int* arr = new int[5]{1, 2, 3, 4, 5};
delete[] arr;  // 注意 delete[]，不是 delete！

// delete 调用析构函数
delete p1;  // 调用 ~Person()，然后释放内存
```

| | `new` / `delete` | `malloc` / `free` |
|---|---|---|
| 来源 | C++ 运算符 | C 标准库函数 |
| 调用构造函数 | ✅ | ❌ |
| 调用析构函数 | ✅ | ❌ |
| 返回类型 | 类型安全指针 | `void*`（需要强转） |
| 数组形式 | `new T[n]` / `delete[] p` | `malloc(n*sizeof(T))` / `free(p)` |
| 失败行为 | 抛 `bad_alloc` 异常 | 返回 `NULL` |

---

<a id="三拷贝控制--三个关键操作"></a>
## 三、拷贝控制 — 三个关键操作

### 3.1 为什么需要拷贝控制？

```cpp
class SimpleString {
    char* data;
    size_t len;
public:
    SimpleString(const char* s)
        : len(strlen(s)), data(new char[len + 1]) {
        strcpy(data, s);
        cout << "构造: \"" << data << "\"" << endl;
    }

    // ===== 拷贝控制三件套 =====

    // 1. 析构函数
    ~SimpleString() {
        cout << "析构: \"" << data << "\"" << endl;
        delete[] data;
    }

    // 2. 复制构造函数
    SimpleString(const SimpleString& other)
        : len(other.len), data(new char[other.len + 1]) {
        strcpy(data, other.data);
        cout << "复制构造: \"" << data << "\"" << endl;
    }

    // 3. 赋值运算符
    SimpleString& operator=(const SimpleString& rhs) {
        cout << "赋值: \"" << data << "\" ← \"" << rhs.data << "\"" << endl;
        if (this != &rhs) {  // 防止自我赋值！
            delete[] data;              // 释放旧资源
            len = rhs.len;
            data = new char[len + 1];  // 分配新资源
            strcpy(data, rhs.data);     // 复制数据
        }
        return *this;
    }

    const char* c_str() const { return data; }
};

// 使用示例
SimpleString s1("Hello");           // 构造
SimpleString s2 = s1;                // 复制构造
SimpleString s3("World");            // 构造
s3 = s1;                             // 赋值
```

### 3.2 三法则（Rule of Three）

> **如果一个类需要析构函数，那它几乎一定需要复制构造函数和赋值运算符。**

这三者共同管理对象拥有的资源（如动态内存、文件句柄等）。

```cpp
class Counter {
    int* count;  // 动态分配的共享计数器

public:
    Counter() : count(new int(0)) {
        cout << "构造: count=" << *count << endl;
    }

    // 三件套
    ~Counter() {
        cout << "析构: count=" << *count << endl;
        delete count;
    }

    Counter(const Counter& other)
        : count(new int(*other.count)) {  // 深拷贝！
        cout << "复制构造: count=" << *count << endl;
    }

    Counter& operator=(const Counter& rhs) {
        if (this != &rhs) {
            *count = *rhs.count;  // 深拷贝值（不复制指针）
        }
        cout << "赋值: count=" << *count << endl;
        return *this;
    }

    void inc() { ++(*count); }
    int get() const { return *count; }
};

// 使用示例
Counter c1;
c1.inc(); c1.inc();

Counter c2 = c1;  // 复制构造
c2.inc();
// c1 和 c2 各自独立（深拷贝）
```

`★ Insight ─────────────────────────────────────`
如果不用深拷贝，默认的浅拷贝会让两个 `Counter` 对象的 `count` 指针指向**同一块内存**。一个对象析构后，另一个对象的 `count` 就成了**悬空指针**（dangling pointer）。这就是为什么管理资源的类必须三件套齐全。
`─────────────────────────────────────────────────`

---

<a id="四explicit-关键字"></a>
## 四、`explicit` 关键字

```cpp
class VecInt {
    int* data;
    size_t sz;
public:
    explicit VecInt(size_t n) : sz(n), data(new int[n]()) {}
    ~VecInt() { delete[] data; }
    size_t size() const { return sz; }
};

void process(const VecInt& v) {}

// 使用示例
VecInt v1(10);   // ✅ 显式调用构造函数
// VecInt v2 = 10;  // ❌ 编译错误！explicit 阻止了隐式转换

process(VecInt(5));  // ✅ 显式创建临时对象
// process(5);       // ❌ 编译错误！不会隐式把 5 转为 VecInt
```

> `explicit` 用于单参数构造函数（包括有默认值的情况），防止编译器"自作聪明"地进行隐式类型转换。

---

## 五、完整示例：自定义动态数组

```cpp
template <class T>
class Vec {
public:
    typedef T* iterator;
    typedef const T* const_iterator;
    typedef size_t size_type;

    Vec() : data(nullptr), avail(nullptr), limit(nullptr) {}

    explicit Vec(size_type n, const T& val = T()) {
        data = new T[n];
        avail = limit = data + n;
        for (size_type i = 0; i < n; ++i) data[i] = val;
    }

    Vec(const Vec& v) {
        size_type n = v.size();
        data = new T[n];
        avail = limit = data + n;
        copy(v.data, v.avail, data);
    }

    ~Vec() { delete[] data; }

    Vec& operator=(const Vec& rhs) {
        if (this != &rhs) {
            delete[] data;
            size_type n = rhs.size();
            data = new T[n];
            avail = limit = data + n;
            copy(rhs.data, rhs.avail, data);
        }
        return *this;
    }

    size_type size() const { return avail - data; }
    T& operator[](size_type i) { return data[i]; }
    const T& operator[](size_type i) const { return data[i]; }

    iterator begin() { return data; }
    const_iterator begin() const { return data; }
    iterator end() { return avail; }
    const_iterator end() const { return avail; }

    void push_back(const T& val) {
        if (avail == limit) grow();
        *avail++ = val;
    }

private:
    iterator data;
    iterator avail;
    iterator limit;

    void grow() {
        size_type new_size = max(2 * size(), size_type(1));
        iterator new_data = new T[new_size];
        iterator new_avail = copy(data, avail, new_data);
        delete[] data;
        data = new_data;
        avail = new_avail;
        limit = data + new_size;
    }
};

// 使用示例：学生成绩管理
struct Student {
    string name;
    double score;
};

ostream& operator<<(ostream& os, const Student& s) {
    return os << s.name << "(" << s.score << ")";
}

Vec<int> numbers;
for (int i = 1; i <= 5; ++i)
    numbers.push_back(i * 10);

Vec<Student> class2024;
class2024.push_back({"Alice", 85.5});
class2024.push_back({"Bob", 72.0});
class2024.push_back({"Charlie", 93.5});

// 拷贝测试
Vec<int> numbers2 = numbers;  // 复制构造
numbers2[0] = 999;
// numbers[0] 未受影响（深拷贝）
```

---

## 六、第六周总结

| 知识点 | 说明 | 类比 C 语言 |
|---|---|---|
| **模板类** | `template<class T> class Vec { ... }` | 为每种类型重写一套代码 |
| **`new` / `delete`** | 动态分配 + 构造/析构 | `malloc` / `free`（不调构造/析构） |
| **析构函数** `~T()` | 对象销毁时自动释放资源 | 手动写 `cleanup()` 函数 |
| **复制构造函数** | `T(const T&)` — 深拷贝 | 手写 `clone()` 函数 |
| **赋值运算符** | `operator=(const T&)` — 注意自我赋值 | 手写 `copy_from()` 函数 |
| **三法则** | 需要析构 → 也需要复制构造 + 赋值 | （只能靠自己遵守纪律） |
| **`explicit`** | 阻止隐式类型转换 | （不存在此机制） |

### 动态内存管理速查

```
创建单个对象: p = new T(args);    delete p;
创建数组:     p = new T[n];       delete[] p;
类中管理内存: 必须提供析构、复制构造、赋值（三法则）
防止隐式转换: explicit 构造函数
防止自我赋值: if (this != &rhs) { ... }
```

> **课后练习**：为 `Vec<T>` 添加 `pop_back()` 和 `clear()` 成员函数。思考：`pop_back()` 需要释放元素的内存吗？`clear()` 和析构函数的区别是什么？
