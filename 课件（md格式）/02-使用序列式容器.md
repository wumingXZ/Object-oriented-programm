# 02-使用序列式容器

## 主要内容

- Hello World程序
- 使用字符串
- 使用批量数据
- 组织程序和数据
- 使用序列式容器
- 使用库算法
- 使用关联式容器

## 使用序列式容器并分析字符串

- 标准库不仅提供了有用的数据结构和函数，它还反映了一种一致性的体系结构：
  - 只要我们知道如何使用一个容器，我们自己就会明白如何使用所有的库容器。

## 把学生分类

- 问题：希望知道哪些学生没通过考试。
- 检测函数：

```cpp
bool fgrade(const Student_info &s){
    return grade(s) < 60 ;
}
```

## 解决问题-最直接方法

- 两个vector：检测每个学生的记录，
  - 一个vector保存及格的学生记录，
  - 另一个保存不及格的学生记录。

```cpp
vector<Student_info> extract_fails(vector<Student_info> &students){
    vector<Student_info> pass, fail;//两个vector
    for(vector<Student_info>::size_type i=0;i!=students.size();++i){
        if(fgrade(students[i])){
            fail.push_back(students[i]);
        }else{
            pass.push_back(students[i]);
        }
    }
    students=pass;
    return fail;
}
//缺点：需要足够的内存来保存每个学生的两份副本。
```

## 解决问题-就地删除元素

```cpp
vector<Student_info> extract_fails(vector<Student_info> &students)
{
    vector<Student_info> fail;//一个vector
    for(vector<Student_info>::size_type i=0;i!=students.size();){
        if(fgrade(students[i])){
            fail.push_back(students[i]);
            students.erase(students.begin()+i);
        }else{
            ++i;
        }
    }
    //students=pass;
    return fail;
}
```

删除第i个元素

循环中会改变

## 顺序访问和随机访问

```cpp
vector<Student_info> extract_fails(vector<Student_info> &students)
{
    vector<Student_info> fail;//一个vector
    for(vector<Student_info>::size_type i=0;i!=students.size();){
        if(fgrade(students[i])){
            fail.push_back(students[i]);
            students.erase(students.begin()+i);
        }else{
            ++i;
        }
    }
    //students=pass;
    return fail;
}
```

## 迭代器

- 迭代器为一个值
- 标识一个容器和容器中的一个元素
- 允许访问元素中保存的值
- 提供在容器元素之间移动的操作
- 约束可用的操作

## 迭代器-顺序访问

```cpp
for(vector<Student_info>::size_type i=0;i!=students.size();++i){
    cout <<students[i].name <<endl;
}

for(vector<Student_info>::const_iterator
    iter=students.begin();iter!=students.end();++iter){
    cout << (*iter).name <<endl;
}
```

随机访问

顺序访问

## 迭代器类型

- 每个标准库容器，比如vector，都定义了两个相关的迭代器类型：

```cpp
container-type::const_iterator
container-type::iterator

for(vector<Student_info>::const_iterator iter=student.begin();
```

只读取访问时

需要改变保存在容器中的值时

这里有 iterator=>const_iterator 的自动转换

## 迭代器操作

```cpp
for(vector<Student_info>::const_iterator
    iter=students.begin();iter!=students.end();++iter){
    cout << (*iter).name <<endl;
}

for(auto iter= students.begin();iter!=students.end();++iter){
    cout << iter->name <<endl;
}

// 或者：
cout << iter-> name <<endl;
```

## erase函数

- students.erase(students.begin()+i)

对支持随机访问的容器有意义

对只支持顺序访问的容器无意义

```cpp
iter=students.erase(iter);
```

erase函数返回指向被删除元素之后跟着的元素的迭代器

## 使用迭代器取代索引

```cpp
vector<Student_info> extract_fails(vector<Student_info> & students)
{
    vector<Student_info> fail;

    for(std::vector<Student_info>::iterator 
        iter=students.begin();iter!=students.end();){
        if(fgrade(*iter)){
            fail.push_back(*iter);
            iter=students.erase(iter);
        }else{
            ++iter;
        }
    }

    return fail;
}
```

erase函数返回指向被删除元素之后跟着的元素的迭代器

## 重新设计数据结构以获得更好的性能

- vector的问题：插入和删除费时
- 解决：使用list

## list

```cpp
#include <list>
list<Student_info> extract_fails(list<Student_info> & students)
{
    list<Student_info> fail;

    for(std::list<Student_info>::iterator 
        iter=students.begin();iter!=students.end();){
        if(fgrade(*iter)){
            fail.push_back(*iter);
            iter=students.erase(iter);
        }else{
            ++iter;
        }
    }
    return fail;
}
```

## list Vs. vector

- list顺序访问比vector稍慢些
- 对于大型数据，list删除和插入要比vector快得多
- vector支持索引，而list不支持
  - vector[i] //right
  - list[i] //error!

## 学生信息排序问题

- 对学生信息进行排序的时候调用students.sort()
  - 标准库中的sort函数不支持list元素排序
- 由于VC6.0的编译器不支持以下的函数：list的成员函数sort(bool cmp)
- 在struct Student_info中重载操作符<：

```cpp
struct Student_info{
    std::string name;
    double midterm, final;
    std::vector<double> homework;
    bool operator<( const Student_info &y)
    { return name < y.name; }
};
```

## 其它问题

- 1、函数max可以用_cpp_max替换
- 2、list<Student_info>::iterator等类型定义可能无法通过编译，可在前面加std，即std::list<Student_info>::iterator

## 主要内容

- 容器
  - vector
  - string
  - list
- 迭代器
  - iterator
- 库算法
- 编写范型函数

## map

- map是一种关联式容器，
  - 包含唯一的键值对，即 (key, value)，
  - 内部会根据key值进行排序；
  - 增加、修改和查询具有对数的时间复杂度

## map-2

```cpp
int main(){
    map<int, string> m1;
    m1.insert({2,"c"});
    m1.insert({4,"c++"});
    m1.insert({5,"python"});
    for(auto iter=m1.begin();iter!=m1.end();iter++)
        cout<<iter->first<<" "<<iter->second<<endl;

    map<int, string> 
    m2{{1,"student"},{4,"teacher"},{3,"boy"},{2,"girl"}};

    for(auto iter=m2.begin();iter!=m2.end();iter++)
        cout<<iter->first<<" "<<iter->second<<endl;

    return 0;
}
```

## map-3

```cpp
int main(){
    map<int, string> m3;

    m3.insert({3,“linux”});
    //C++11统一初始化

    m3.emplace(2,“windows”); 
    //最高效,避免临时对象

    m3.insert(std::pair<int32_t, std::string>(1,“mac”)); //传统pair方式
    m3.insert(std::make_pair(5,“unix”)); //make_pair方式

    for(auto iter=m3.begin();iter!=m3.end();iter++)
        cout<<iter->first<<" "<<iter->second<<endl;

    map<int, string> m4 = m1;
    m4[10] = "shell";
    //元素的添加

    m4[7] = "Rust";

    m4[5] = "Java";
    //会替换(5, python)

    for(auto iter=m4.begin();iter!=m4.end();iter++)
        cout<<iter->first<<" "<<iter->second<<endl;

    return 0;
}
```

## map常用方法

方法 | 说明
--- | ---
at() | 元素访问
operator[] | 元素访问
iterator | 可以访问元素，也可以遍历字典中的元素
empty | 数组判空
size | 字典大小
max_size | 最大容量
clear | 清空map的大小
swap | 交换两个map的内容
= | 直接赋值
insert | 插入元素
emplace | emplace 是 C++11 引入的新特性，用于在容器中直接构造元素，避免不必要的临时对象创建和拷贝
erase | 删除元素
